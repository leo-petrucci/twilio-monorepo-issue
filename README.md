This is an experiment for a monorepo of Twilio flex plugins. It's failing after install.

## To reproduce

Install with `yarn`

```
yarn
```

Open `plugins/team-filters/src/SamplePlugin.tsx`, dependencies will show as not found.
